# Lambda function dependencies for dual routing system
# These dependencies will be packaged with the Lambda deployment

# AWS SDK for Python (usually included in Lambda runtime, but specifying for completeness)
boto3>=1.26.0
botocore>=1.29.0

# JSON handling and utilities (usually included in Python standard library)
# requests>=2.28.0  # Uncomment if HTTP requests are needed

# Additional utilities that might be useful
# python-dateutil>=2.8.0  # Usually included with boto3
# urllib3>=1.26.0  # Usually included with boto3

# Note: Most dependencies are already available in the Lambda Python runtime
# Only add dependencies here that are not included in the standard runtime
# Keep the deployment package as small as possible for better performance